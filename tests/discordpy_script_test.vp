static $num = randnum(0, 3)
if ($num is 1) {
    say("number is 1")
} else if (num is 2) {
    say("number is 2")
} else {
    say("number is other")
}
say("test")
$v = "hi " + $ctx.author.name
if ($v in $ctx.content) {
    $ctx.send("Hello, " + $ctx.author.name)
}
say($ctx.send("Blah"))
$n = string($num)
$v = "Number is " + $n
say($v)